# PyTorch packages (CUDA 12.1) - Compatible with Python 3.9, 3.10, 3.12
# Note: Extra index must be specified before torch packages
--extra-index-url https://download.pytorch.org/whl/cu121

torch==2.5.1+cu121
torchvision==0.20.1+cu121
torchaudio==2.5.1+cu121

# Core dependencies
python-dotenv
chromadb
tiktoken
sentence-transformers
transformers>=4.45.0  # Minimum version for Llama 3.2 support
accelerate
bitsandbytes  # For 4-bit quantization (optional, speeds up Llama)

# Document processing
unstructured[pdf]
unstructured-client

# Image and data processing
pillow
lxml
